# Vision-Transformer
Built a Vision Transformer (ViT) image classifier entirely from scratch — no pretrained backbones or Hugging Face implementations — implementing patch embeddings, self-attention, and transformer encoders manually in PyTorch to classify CIFAR-10 images.
